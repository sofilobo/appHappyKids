@model IEnumerable<Infraestructure.Models.EstadoProducto>

@{
    ViewBag.Title = "EstadoProducto";
}

<h2>EstadoProducto</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEstado }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEstado }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEstado })
        </td>
    </tr>
}

</table>
